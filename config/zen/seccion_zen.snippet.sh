# ---- Pega seccion_zen() en setup_master.sh y registra en el dispatcher ----
#   SECCIONES+=("zen")
#   SEC_DESC+=("Navegador Zen: tema Horus, prefs y extensiones")
#   SEC_SUDO+=(0)
#   SEC_RED+=(0)
# (la funcion es la misma logica de despliegue que este script)
